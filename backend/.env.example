# Gemini (existing)
# GEMINI_API_KEY=

# Firebase / GCP — project: resumeai-d3bd5
FIREBASE_PROJECT_ID=resumeai-d3bd5
GOOGLE_CLOUD_PROJECT=resumeai-d3bd5

# One of the following for Firebase Admin (service account with Firestore access):
# Path to downloaded service account JSON (do not commit the file; keep it outside the repo or gitignored):
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/resumeai-d3bd5-xxxxx.json

# Or inline JSON (useful for Cloud Run secrets — escape newlines carefully):
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"resumeai-d3bd5",...}
